import 'package:flutter/material.dart';

class SetWorkplaceView extends StatelessWidget {
  const SetWorkplaceView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Text('dddd')),
    );
  }
}
